{% extends 'base.html.twig' %}

{% block title %}Edit Produtos{% endblock %}

{% block body %}
    <h1>Edit Produtos</h1>

    {{ include('produtos/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('produtos_index') }}">back to list</a>

    {{ include('produtos/_delete_form.html.twig') }}
{% endblock %}
